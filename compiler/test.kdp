taken = "x" end
open = "o" end

lattice = TwoDArray 100, 100 end

# Brownian motion walk in a lattice
Walk x, y do
  v = rand(0, 1)
  if v == 0 do
    return x + 1
  else do
    return y + 1
  end
end

Collision x, y do
  critical do
    v = Walk(x, y) == taken
    return v
  end
end

Stick x, y do
end

Simluate do
  x = rand (0, size_of_lattice_x)
  y = rand (0, size_of_lattice_y)

  for i in timesteps (100) do
    pos = Walk x, y
    col = Collision pos
    Stick col
  end
end
